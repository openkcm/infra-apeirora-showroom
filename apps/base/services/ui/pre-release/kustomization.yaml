apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: ui-base-pre-release-values
    files:
      - ./resources/ui-envoy-config.yaml
      - ./resources/config.json
