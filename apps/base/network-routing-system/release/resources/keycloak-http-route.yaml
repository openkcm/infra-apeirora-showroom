resources:
  keycloak-http-route:
    # JWT based HTTPRoute
    apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    metadata:
      name: keycloak-http-route
    spec:
      parentRefs:
        - name: ingress-gateway
          namespace: envoy-gateway-system
      hostnames:
        - "ing.<replace>.kms20.shoot.canary.k8s-hana.ondemand.com"
      rules:
        - backendRefs:
            - group: ""
              kind: Service
              name: keycloak
              namespace: keycloak
              port: 8080
              weight: 1
          matches:
            - path:
                type: PathPrefix
                value: /auth
          filters:
            - type: ResponseHeaderModifier
              responseHeaderModifier:
                remove:
                  - "L5d-Client-Id"
