apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openbao-local-secret-sync
spec:
  interval: 10m
  releaseName: openbao-local-secret-sync
  chart:
    spec:
      chart: ./tools/charts/generic
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: false
    remediation:
      retries: -1
      remediateLastFailure: true
  upgrade:
    remediation:
      retries: -1
      remediateLastFailure: true
  values:
    resources:
      serviceAccount:
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: openbao-local-secret-sync
          namespace: openbao
      role:
        apiVersion: rbac.authorization.k8s.io/v1
        kind: Role
        metadata:
          name: secrets-reader-openbao-sync
          namespace: openbao
        rules:
          - apiGroups: [""]
            resources:
              - secrets
            verbs:
              - get
              - list
              - watch
      rolebinding:
        apiVersion: rbac.authorization.k8s.io/v1
        kind: RoleBinding
        metadata:
          name: secrets-reader-openbao-sync
          namespace: openbao
        subjects:
          - kind: ServiceAccount
            name: openbao-local-secret-sync
            namespace: openbao
        roleRef:
          kind: Role
          name: secrets-reader-openbao-sync
          apiGroup: rbac.authorization.k8s.io
      secretStore:
        apiVersion: external-secrets.io/v1
        kind: ClusterSecretStore
        metadata:
          name: openbao-local-secret-sync
        spec:
          provider:
            kubernetes:
              server:
                url: "https://kubernetes.default"
                caProvider:
                  type: "ConfigMap"
                  name: "kube-root-ca.crt"
                  key: "ca.crt"
                  namespace: openbao
              remoteNamespace: openbao
              auth:
                serviceAccount:
                  name: openbao-local-secret-sync
                  namespace: openbao
      es-cluster-cert:
        apiVersion: external-secrets.io/v1
        kind: ExternalSecret
        metadata:
          name: local-secret-sync-cluster-cert
          namespace: services
        spec:
          secretStoreRef:
            kind: ClusterSecretStore
            name: openbao-local-secret-sync
          target:
            name: cluster-cert
            creationPolicy: Owner
          dataFrom:
            - extract:
                key: cluster-cert
