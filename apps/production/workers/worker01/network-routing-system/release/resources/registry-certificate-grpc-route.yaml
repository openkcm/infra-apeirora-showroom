resources:
  registry-certificate-grpc-route:
    # Certificate based GRPCRoute
    apiVersion: gateway.networking.k8s.io/v1
    kind: GRPCRoute
    metadata:
      name: registry-certificate-grpc-route
    spec:
      hostnames:
        - "ing.cert.worker01.kms.shoot.gardener.cc-one.showroom.apeirora.eu"
