resources:
  envoy-proxy-config:
    apiVersion: gateway.envoyproxy.io/v1alpha1
    kind: EnvoyProxy
    metadata:
      name: envoy-proxy-config
    spec:
      provider:
        kubernetes:
          envoyService:
            annotations:
              dns.gardener.cloud/dnsnames: ing.worker01.kms.shoot.gardener.cc-one.showroom.apeirora.eu
              cert.gardener.cloud/dnsnames: ing.worker01.kms.shoot.gardener.cc-one.showroom.apeirora.eu
              cloud.google.com/load-balancer-type: "External"
            ports:
              - name: https
                port: 443
                targetPort: 10443
                protocol: TCP
              - name: kmip
                port: 5697
                targetPort: 5697
                protocol: TCP
          envoyHpa:
            minReplicas: 3
            maxReplicas: 5
          envoyDeployment:
            replicas: 3
            pod:
              annotations:
                config.linkerd.io/skip-inbound-ports: 10001,10443

