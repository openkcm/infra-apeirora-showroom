apiVersion: v1
data:
  config.yaml: |
    apiVersion: apiserver.config.k8s.io/v1beta1
    kind: AuthenticationConfiguration
    jwt:
    - issuer:
        url: https://discovery.ingress.garden.canary.k8s.ondemand.com/projects/kms20/shoots/c0dca5cc-8f7e-4bbe-b164-6673b1defe7a/issuer
        audiences:
        - downstream-cluster
      claimMappings:
        username:
          claim: sub
          prefix: "infra-cluster-oidc:"
kind: ConfigMap
metadata:
  name: showroominfra-issuer-configmap
  namespace: garden-kms20
